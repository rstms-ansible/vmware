vm_os: OpenBSD
vm_version: 7.5
vm_secrets_file: example_secrets.yml
vm_filesystem_dir: example_filesystems/openbsd
netboot_packages:
  - figlet
  - bash
netboot_command: "hostname -s | /usr/local/bin/figlet -f slant >/etc/motd"
netboot_fullscreen: true
